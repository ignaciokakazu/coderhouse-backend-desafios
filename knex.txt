npm i install knex mysql
npx knex init
en knexfile.js

  development: {
    client: 'mysql',
    connection: {
      database: 'prueba',
      user:     'root',
      password: '',
      database: 'prueba'
    },
    migrations: {
      directory: __dirname + '/db/migrations'
    },
    seeds: {
      directory: __dirname + '/db/seeds'
    }    
  },


npx knex migrate:make nombre

en migrations exports.up (la creación), en down drop

npx knex migrate:latest --env development
npx knex migrate:rollback


npx knex seeds:alimentar

45:31